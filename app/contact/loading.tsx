// This shows the loading state when data is fetched from an API.
// to make this work, file naming convention must be followed.

export default function LoadingState() {
	return <h1>Loading...</h1>
}